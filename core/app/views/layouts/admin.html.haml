!!! 5
%html
	%head
		= hook :admin_inside_head do
			= render "admin/shared/admin_head"
	%body.railsbike_admin
		%header
			#admin_menu
				%ul.nav
					%li.admin-logo
						%span.railsbike
							%b> Rails
							bike
							%b> CMS
					= hook :admin_tabs do
						=render "admin/shared/admin_tabs"
					%li.right
						= hook :shared_login_bar do
							%span hello!
		#wrapper					
			#sub_menu
				= yield :sub_menu
			#content
				- if flash[:error]
					.flash.error
						= flash[:error]
				= yield